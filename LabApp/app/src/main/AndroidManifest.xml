<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.tcc.labapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TabPaciente" />
        <activity android:name=".TabMedico" />
        <activity android:name=".TelaCadastro" />
        <activity android:name=".TelaInicialPaciente" />
        <activity android:name=".TelaAjuda" />
        <activity android:name=".TelaInicialMedico" />
        <activity android:name=".TelaAgendamento"></activity>
    </application>

</manifest>